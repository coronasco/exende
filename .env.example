# Server-only origins. Never expose these variables to client bundles.
EXENDE_CONTROL_PLANE_URL=http://127.0.0.1:8787
EXENDE_DATA_API_URL=http://127.0.0.1:8788

# Public, credential-free preview origin. Defaults to https://data.exende.dev.
# EXENDE_PUBLIC_DATA_ORIGIN=https://data.exende.dev
